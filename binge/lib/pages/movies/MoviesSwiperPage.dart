import 'package:flutter/material.dart';

class MoviesSwiperPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Movies Swiper Page'),
    );
  }
}
