import 'package:flutter/material.dart';

class SwiperPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
     
      body: Center(
        child: Text('Swiper Page Content'),
      ),
    );
  }
}
