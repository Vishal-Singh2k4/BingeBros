import 'package:flutter/material.dart';

class GamesHomePageContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Welcome to the Games Home Page'),
    );
  }
}
